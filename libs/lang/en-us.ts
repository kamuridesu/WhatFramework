interface Translations {
    commands: string;
    closingTagMissing: string;
  }
  
  const TRANSLATIONS: Translations = {
    commands: "Commands",
    closingTagMissing: "Error! Some categories do not have closing tags!",
  };
  
  export { TRANSLATIONS };
  